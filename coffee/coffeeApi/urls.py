from django.urls import path
from . import views

urlpatterns = [
    # path('', views.APICoffeeNoPk, name='action without id'),
    # path('<int:pk>', views.APICoffeePk, name='action with id'),
    # path('coffee-list/', views.ShowAll, name='coffee-list'),
    # path('coffee-detail/<int:pk>/', views.ViewCoffee, name='coffee-detail'),
    # path('coffee-add/', views.AddCoffee, name='coffee-add' ),
    # path('coffee-update/<int:pk>', views.UpdateCoffee, name='coffee-update'),
    # path('coffee-modify/<int:pk>', views.ModifyCoffee, name='coffee-modify'),
    # path('coffee-del/<int:pk>', views.DeleteCoffee, name='coffee-delete'),
]
